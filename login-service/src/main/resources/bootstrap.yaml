# 配置端口信息
server:
  port: 6001

spring:
  application:
    # 服务名称
    name: login-service
  profiles:
    # 当前环境
    active: dev
  cloud:
    # nacos 注册中心配置
    nacos:
      # nacos 注册中心地址
      server-addr: 49.233.56.155:8848
      config:
        # 配置文件后缀名
        file-extension: yaml
    loadbalancer:
      ribbon:
        enabled: false